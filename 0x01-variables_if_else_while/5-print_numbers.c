#include <stdio.h>

/**
 * main - printing numbers 0 to 10
 *
 *Return: numbers listed in order
 */

int main(void)
{
int x;

for (x = 0; x <= 9; x++)
{
printf("%d", x);
}
printf("\n");
return (0);
}
