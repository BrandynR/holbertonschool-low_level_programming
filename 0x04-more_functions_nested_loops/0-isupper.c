#include "header.h"

/**
 *_isupper - Checks for upper case letter
 *@c: represents a letter
 *Result: Returns 1 c is uppercase, 0 if lowercase
 */

int _isupper(int c);
{
	if (n = 'A'; n <= 'Z')
		return (1);
	else
		return (0);
}
