#include "holberton.h"
/**
 *rev_string - reverses a string
 *@s: string
 *Return: void
 */

void rev_string(char *s)
{
int len = 0;

while (s[len] != '\0')
{
*start = 0;
*end = len - 1;
temp = *end;
*end = *start;
*start = temp;
}
for (i = 0; i <= len / 2; i++)
{
start++;
end--;
}
}
