#include "holberton.h"

/**
 *_puts - prints a string
 *@str: the tsring to be printed
 *Return:
 */

void _puts(char *str)
{
char s[100];
int c = 0;

for (c = 0; s[c] != '\0'; c++)
putchar(c)

putchar('\n')
